// RequestControls removed -- participants no longer have play/pause request buttons.
export default function RequestControls(_props: {
  roomCode: string;
  nickname: string;
  isHost: boolean;
}) {
  return null;
}
